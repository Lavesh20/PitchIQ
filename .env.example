# Copy to .env and fill in. .env is gitignored and must never be committed.

# https://www.football-data.org/client/register
FOOTBALL_DATA_API_TOKEN=

# GitHub personal access token, scoped to this repository only.
GITHUB_PAT=
